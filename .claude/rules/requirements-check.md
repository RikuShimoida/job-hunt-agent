## 実装前の要件確認ルール

実装指示を受けた場合、以下のすべてが明確でない限り、実装に着手せず質問攻めすること。
ユーザーが「OK」「進めて」と回答するまで実装を開始してはならない。

- 対象コンポーネント（どのパッケージ・コマンド・エンドポイントか）
- 期待する動作（何を入力したら何が出力・変化するか）
- インターフェース（公開する関数・型・CLIフラグ・APIの入出力契約）
- 影響範囲（既存機能・既存の公開 API への破壊的変更の有無）
- 受入条件（何をもって完了とするか。`go test` で検証可能な形で）
